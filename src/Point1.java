
public class Point1 {
	public Point(int x, int bx) 
	{ 
		this.x = x; 
		y = bx; 
	} 
	/* 
	 * In the code above which is more right? 
	 * a) this.x = x; 
	 * b) y = bx; 
	 * c) Both is correct. 
	 * d) None is correct. 
	 */
}
